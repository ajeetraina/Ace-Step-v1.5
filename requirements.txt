# ACE-Step v1.5 - ARM64 Optimized Requirements for Apple Silicon
# Core ML Framework with Apple Silicon MPS support
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0
torchaudio>=2.1.0
torchvision>=0.16.0

# Apple Silicon optimized scientific computing
numpy>=1.24.0
scipy>=1.11.0

# Audio processing libraries with ARM64 support
librosa>=0.10.0
soundfile>=0.12.0
resampy>=0.4.0
audioread>=3.0.0

# Gradio web interface
gradio>=4.0.0
gradio-client>=0.7.0

# HuggingFace ecosystem with ARM64 support
transformers>=4.35.0
diffusers>=0.21.0
accelerate>=0.24.0
datasets>=2.14.0
tokenizers>=0.14.0

# vLLM with ARM64 optimizations
# Note: Using CPU-optimized version for Apple Silicon
vllm>=0.2.0

# Environment and configuration
python-dotenv>=1.0.0
omegaconf>=2.3.0
hydra-core>=1.3.0

# Image and data processing
pillow>=10.0.0
opencv-python-headless>=4.8.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Utilities and development
tqdm>=4.65.0
wandb>=0.15.0
tensorboard>=2.14.0
rich>=13.0.0

# Flash Attention (CPU fallback for Apple Silicon)
# Note: Flash attention has limited Apple Silicon support
# Using CPU-optimized alternatives
einops>=0.7.0
triton>=2.1.0; platform_machine != "arm64"

# Audio format support
ffmpeg-python>=0.2.0

# Optional: Metal Performance Shaders optimizations
# These are automatically available on macOS with PyTorch MPS support

# Development dependencies (optional)
pytest>=7.4.0
black>=23.0.0
ruff>=0.1.0

# Jupyter support for development
jupyter>=1.0.0
ipywidgets>=8.0.0